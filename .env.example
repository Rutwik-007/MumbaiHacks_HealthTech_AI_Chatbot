# Generate a random secret: https://generate-secret.vercel.app/32 or `openssl rand -base64 32`
AUTH_SECRET=****

# OpenAI API Key for GPT models (required)
# Get your API key from: https://platform.openai.com/api-keys
# Used for: GPT-5 chat, GPT-4o-mini translation, text-embedding-3-small
OPENAI_API_KEY=****

# ===== DISEASE AWARENESS PLATFORM CONFIG (ALL FREE!) =====

# Default language: en, hi, mr, pa
DEFAULT_LANGUAGE=en

# Default user role: citizen, asha, officer  
DEFAULT_USER_ROLE=citizen

# ChromaDB Configuration (FREE - runs in-process, no Docker needed!)
# Leave empty to use in-memory storage (default)
# CHROMA_PERSIST_DIRECTORY=./chroma-data

# Vector embedding model (uses OpenAI key above)
# text-embedding-3-small is the cheapest option
EMBEDDING_MODEL=text-embedding-3-small

# Translation model (uses OpenAI key above)
# gpt-4o-mini is cost-effective for translation
TRANSLATION_MODEL=gpt-4o-mini

# ===== END DISEASE AWARENESS CONFIG =====

# The following keys below are automatically created and
# added to your environment when you deploy on vercel

# AI Gateway API Key (required for non-Vercel deployments)
# For Vercel deployments, OIDC tokens are used automatically
AI_GATEWAY_API_KEY=****


# Instructions to create a Vercel Blob Store here: https://vercel.com/docs/storage/vercel-blob
BLOB_READ_WRITE_TOKEN=****

# Instructions to create a PostgreSQL database here: https://vercel.com/docs/storage/vercel-postgres/quickstart
POSTGRES_URL=****


# Instructions to create a Redis store here:
# https://vercel.com/docs/redis
REDIS_URL=****
